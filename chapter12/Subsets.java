/**
 * Write a method subsets that uses recursive backtracking 
 * to find every possible sub-list of a given list. A 
 * sub-list of a list L contains 0 or more of L's elements. 
 * Your method should accept a List of strings as its parameter 
 * and print every sub-list that could be created from elements 
 * of that list, one per line.
 */

 