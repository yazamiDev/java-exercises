/**
 * Write a recursive method indexOf that accepts two Strings as 
 * parameters and that returns the starting index of the first 
 * occurrence of the second String inside the first String (or 
 * -1 if not found). The table below lists several calls to your 
 * method and their expected return values. Notice that case matters, 
 * as in the last example that returns -1.
 */